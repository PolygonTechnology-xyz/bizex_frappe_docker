export APPS_JSON_BASE64=$(base64 -w 0 apps_bizex_hdc.json)

echo "$APPS_JSON_BASE64" | base64 --decode > apps_decoded.json

docker build --no-cache \
    --build-arg=FRAPPE_PATH=https://github.com/frappe/frappe \
    --build-arg=FRAPPE_BRANCH=version-15 \
    --build-arg=APPS_JSON_BASE64=$APPS_JSON_BASE64 \
    --tag=abbirku/bizex_hdc:v1.0.8 \
    --file=images/layered/Containerfile .

docker push abbirku/bizex_hdc:v1.0.8

make up

docker compose -f hdc.yml logs -f create-site

bench --site hdc_site set-admin-password admin